import { Bacon } from 'sigh-core'
import Event from 'sigh/lib/Event'

import process from '../'

require('source-map-support').install()
require('chai').should()

describe('sigh-process', () => {
  xit('should start a process when an event is passed through', () => {
    // TODO:
  })

  xit('should restart a process after the second event', () => {
    // TODO:
  })

  xit('should delay the events until the process has output to the console', () => {
    // TODO:
  })
})
